name: testando a regra de protection role

on:
  create


jobs:
  print-branch-or-tag-creation:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso sobre a criacao da branch ou tag
        run: echo ¨Uma nova branch ou tag foi criada no repositorio.¨